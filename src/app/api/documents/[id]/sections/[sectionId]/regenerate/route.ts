import { notImplemented } from "@/lib/server/http";

export const runtime = "nodejs";

export async function POST() {
  return notImplemented(
    "Section regeneration will be available after the LLM provider is wired.",
  );
}
